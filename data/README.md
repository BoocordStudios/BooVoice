# Runtime data

BooVoice creates `store.json` in this directory when it starts. The file contains
Discord guild, channel, and user IDs and is intentionally excluded from Git.

Back up `store.json` if the bot's server configuration needs to survive a host
migration. Do not publish it or include it in bug reports.
